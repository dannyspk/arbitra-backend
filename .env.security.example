# =============================================================================
# SECURITY ENVIRONMENT VARIABLES
# Copy this file to .env and fill in your values
# =============================================================================

# JWT Authentication
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=REPLACE_WITH_YOUR_GENERATED_SECRET_KEY

# API Key Encryption
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=REPLACE_WITH_YOUR_GENERATED_FERNET_KEY

# Environment
ENVIRONMENT=development  # Use 'production' when deployed

# CORS Allowed Origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://yourdomain.com

# =============================================================================
# EXISTING BINANCE CONFIGURATION (Keep your existing values)
# =============================================================================
BINANCE_API_KEY=your_existing_binance_key
BINANCE_API_SECRET=your_existing_binance_secret

# =============================================================================
# OTHER EXISTING ENVIRONMENT VARIABLES
# (Keep any other variables you already have)
# =============================================================================
