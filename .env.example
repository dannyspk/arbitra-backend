# Example env vars for DEX live execution (do NOT commit real keys)
# Enable live on-chain swaps (must be set to '1' to allow)
ALLOW_LIVE_ONCHAIN=0
# RPC node URL (e.g., Infura/Alchemy)
RPC_URL=https://rpc.example
# Private key for signing (use testnet only!)
PRIVATE_KEY=your_private_key_here
# Uniswap router & factory addresses (testnet)
UNISWAP_ROUTER_ADDRESS=0x...
UNISWAP_FACTORY_ADDRESS=0x...

# API key for POST /execute calls
ARB_API_KEY=demo-key
